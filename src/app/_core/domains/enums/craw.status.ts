export enum CrawStatus {
    Waiting = 0,
    Running,
    Complete
}

export enum CrawKeywordStatus {
    Disable = 0,
    Enable,
}

export enum CrawWebsiteStatus {
    Disable = 0,
    Enable,
}

export enum CrawRunningStatus {
    Pause = 0,
    Running,
    Complete
}